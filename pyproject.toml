[project]
name = "ai-companion"
version = "0.1.0"
description = "AI companion focused on persistent memory, retrieval, contradiction handling, and personality consistency."
requires-python = ">=3.10,<3.11"
dependencies = [
    "openai>=3.6.0",
    "pinecone>=9.1.0",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "python-dotenv>=1.2.3",
    "sqlmodel>=0.0.42",
]
